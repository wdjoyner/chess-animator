# Chess Game Animator — Python dependencies
# Install with: pip install -r requirements.txt
#
# Also required (not pip-installable):
#   Stockfish binary — https://stockfishchess.org/download/
#   LaTeX distribution (for Manim text rendering) — e.g. TeX Live or MiKTeX

# Core animation
manim>=0.18.0
manim-chess>=0.1.0

# Chess logic and PGN/engine handling
chess>=1.10.0

# Standard library modules used (no install needed):
#   argparse, dataclasses, io, json, math, os, pathlib,
#   re, subprocess, sys, time, typing
